# Frontend local-development example.
# Copy to `frontend/.env.local` for local overrides.

# Leave blank for local development with separate backend service ports below.
# Set only when a local gateway/proxy exposes all backend routes through one origin.
VITE_API_URL=

# Optional local-development fallbacks used only when VITE_API_URL is unset.
VITE_USER_SERVICE_URL=http://localhost:3001
VITE_HOTEL_SERVICE_URL=http://localhost:3002
VITE_BOOKING_SERVICE_URL=http://localhost:3003
VITE_PAYMENT_SERVICE_URL=http://localhost:3004

# Bella currently redirects to provider-hosted checkout URLs returned by the backend.
# A publishable provider key is not required until the frontend moves to a client SDK/tokenization flow.
